import { LastIngredientDirective } from './last-ingredient.directive';

describe('LastIngredientDirective', () => {
  it('should create an instance', () => {
    const directive = new LastIngredientDirective();
    expect(directive).toBeTruthy();
  });
});
