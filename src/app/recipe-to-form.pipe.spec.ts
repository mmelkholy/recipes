import { RecipeToFormPipe } from './recipe-to-form.pipe';

describe('RecipeToFormPipe', () => {
  it('create an instance', () => {
    const pipe = new RecipeToFormPipe();
    expect(pipe).toBeTruthy();
  });
});
